/**
 * Constant object that holds keys for local storage and cookie storage.
 */
export const STORAGE_KEYS = Object.freeze({
  THEME: 'theme',
});

/**
 * Object containing media query breakpoints.
 */
export const MEDIA_BREAKPOINTS = Object.freeze({
  XSMALL: '320px',
  SMALL: '576px',
  MEDIUM: '768px',
  LARGE: '992px',
  XLARGE: '1200px',
});
